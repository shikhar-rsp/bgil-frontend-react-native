/**
 * Phosphor icons for React Native — same icon family as `@phosphor-icons/react` on web.
 *
 * Requires `react-native-svg` in the host app. No component icons are wired here yet;
 * import icons from this barrel or from `phosphor-react-native` directly.
 */
export * from 'phosphor-react-native';
